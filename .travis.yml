language: python
python:
    - "3.5"
before_install:
    - sudo apt-get update
    - sudo apt-get install -y poppler-utils
install:
    - make
script:
    - make test
group: stable
dist: precise
sudo: required
os: linux
